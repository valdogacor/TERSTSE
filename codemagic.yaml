workflows:
  ios-build:
    name: iOS Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build IPA
        script: flutter build ipa --export-method ad-hoc --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa
